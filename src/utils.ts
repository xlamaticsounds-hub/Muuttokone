// utils.ts

export function calculatePrice(weight: number, carat: number): number {
  // TODO: Implement the logic to calculate the price based on weight and carat
  return 0;
}

export function generateDiscordMessage(
  weight: number,
  carat: number,
  spotPrice: number,
  offer: number,
  potentialProfit: number,
  link: string
): string {
  // TODO: Implement the logic to generate a Discord message
  return '';
}